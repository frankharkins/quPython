"""
Use this module to import the `qupython.qubit.BitPromise` type for annotations.
Since you shouldn't instantiate a `BitPromise` yourself, this makes it clear
the import is just for annotations.

```python
from qupython.typing import BitPromise
```
"""
from qupython.qubit import BitPromise
